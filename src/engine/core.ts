import { gameService } from '../state/stateMachine';

/**
 * Method that console logs string to make sure the stuff is working properly
 */
export const logStuff = () => {
    console.log('Game Engine Core Test');
};

/**
 * Method that console logs string to make sure the stuff is working properly vol.2
 */
export const logOtherStuff = () => {
    console.log('Game Engine Core Test .......2');
};
